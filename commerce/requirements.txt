# Commerce service — pinned to weespas's proven SYNC stack (commerce is request/response;
# async/asyncpg is reserved for the realtime mobility service per the architecture doc §4).
fastapi==0.104.1
uvicorn==0.24.0
SQLAlchemy==2.0.23
psycopg2-binary==2.9.9
GeoAlchemy2==0.14.3        # PostGIS Geometry/Geography column types for SQLAlchemy 2.0
pydantic==2.5.0
pydantic-settings==2.1.0
PyJWT==2.9.0              # RS256 VERIFY only — matches the audited InSAR verifier (not jose)
cryptography==46.0.7      # RSA primitives behind PyJWT RS256
redis==7.4.0             # pinned for the DEFERRED revocation denylist (settlement path); unused this increment
pytest==8.3.4
httpx==0.27.2
