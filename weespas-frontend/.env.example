# Weespas frontend — environment template. Copy to `.env`.
# Vite only exposes vars prefixed with VITE_ to the client. No secrets here.

# Base URL of the Weespas backend API.
VITE_API_BASE_URL=http://localhost:8000/api/v1
